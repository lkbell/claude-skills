interface:
  display_name: "Preflight"
  short_description: "Verify projects are ready before execution"
  default_prompt: "Use $preflight to verify this project and produce a launch-ready plan and execution brief."
